---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## CI/CD Auto-Recreation Feature

The `auto_recreate` attribute allows builders to be automatically recreated if they exist in Terraform state but are missing from Docker. This is particularly useful in CI/CD environments where Docker builders may be cleaned up between runs.

## Example Usage

### Basic Builder

{{tffile "examples/resources/docker_buildx_builder/resource-basic.tf"}}

### Auto-Recreation Enabled (CI/CD)

{{tffile "examples/resources/docker_buildx_builder/resource-auto-recreate.tf"}}

### Mixed Usage (Different Environments)

{{tffile "examples/resources/docker_buildx_builder/resource-mixed-usage.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the following syntax:

{{codefile "shell" "examples/resources/docker_buildx_builder/import.sh"}}


