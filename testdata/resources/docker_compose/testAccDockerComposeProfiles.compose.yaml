services:
  app:
    image: busybox:1.35.0
    command: ["sh", "-c", "sleep 300"]
    environment:
      COMPOSE_MESSAGE: ${COMPOSE_MESSAGE}

  optional:
    image: busybox:1.35.0
    command: ["sh", "-c", "sleep 300"]
    profiles:
      - extras